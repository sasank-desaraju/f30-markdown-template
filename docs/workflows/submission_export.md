# Submission Export

The synthetic helper script can build separate PDF attachments:

```bash
./submission_export.sh
```

This requires a working Quarto PDF toolchain, including a LaTeX engine.
The generated files go to `submission_pdfs/`.

For a real submission, confirm the exact attachment names, page limits, and opportunity-specific requirements before relying on any script.

